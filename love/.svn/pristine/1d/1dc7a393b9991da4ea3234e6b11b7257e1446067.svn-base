package cn.service;

import cn.pojo.Useri;

public interface RegisterService {
    /**
     * 注册业务：
     * 手机号查重验证，
     * @param phone
     * @return
     */
    String Register (String phone);

    /**
     * 注册业务：
     * 获取验证码的业务接口
     * @param iptvernum
     * @return
     */
    String Verifynum(String iptvernum);

    /**
     *注册业务：
     * 添加更多信息的业务接口
     * 完成注册
     * @param u
     * @return
     */
    String lnsertinf(Useri u);
}
